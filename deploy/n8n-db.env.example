# Install as /etc/emecworks/n8n-db.env with owner root:root and mode 0600.
# Never commit the real file or reuse a password from another service.
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=REPLACE_WITH_A_RANDOM_DATABASE_PASSWORD